## YouTube video optimal publish time

 
## Docker

- `docker build -t view-rush -f Dockerfile .`
- `docker run view-rush `

- `docker compose up --watch`
- `docker compose down` or `docker compose down -v`